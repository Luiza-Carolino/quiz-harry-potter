import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.quiz',
  appName: 'Quiz Harry Potter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
